# Instawards SOW — delivery status

Scope: the three testnet deliverables committed in the Instawards SOW (Stellar Foundation, Brazil chapter). Audited against `main` at `43fbbc5`, open PRs, unmerged branches, and the sibling repos `mutav`, `mutav-stellar`, `mutav-pulse`.

**Overall: ~50%.** Remaining work is concentrated in two unwritten modules (`convex/guarantees/`, `coverOperations`) plus wiring and evidence artifacts.

| Deliverable                              | Status |
| ---------------------------------------- | ------ |
| 1. Guarantee & Default Lifecycle Backend | ~45%   |
| 2. Agency Transparency Dashboard         | ~65%   |
| 3. MUTAV Reserve Admin Panel             | ~30%   |

---

## 1. Guarantee & Default Lifecycle Backend (testnet)

### Done

| Item                                                                                 | Evidence                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Delinquency-notice state machine, guarded                                            | `convex/delinquencies/machine.ts` — `open → resolved / canceled`, `assertTransition` returning `Result` with `SELF_TRANSITION` / `TERMINAL_STATE` / `ILLEGAL_TRANSITION`                                                                                                                                                 |
| `assertTransition`-before-patch enforced on every write                              | `convex/delinquencies/mutations.ts`                                                                                                                                                                                                                                                                                      |
| Notice lifecycle table with resolution and cancellation envelopes                    | `convex/schema.ts:452` `contractDelinquencyNotices`, 4 indexes                                                                                                                                                                                                                                                           |
| Cover-commit event with operator, timestamp, reference, audit                        | `convex/delinquencies/mutations.ts:318` `staffMarkResolvedByCover`, role >= `compliance`, `appendStaffAudit`                                                                                                                                                                                                             |
| Dismissal / dispute event                                                            | `convex/delinquencies/mutations.ts:404` `staffMarkCanceledByDismissal`                                                                                                                                                                                                                                                   |
| Guarded production status transition (template for activate/close)                   | `convex/contracts/useCases.ts:1031-1074` `cancelProposal` — precondition guard + `replaceContractAggregates` + `contractHistory` + `appendAuditEntry`                                                                                                                                                                    |
| Lifecycle bookkeeping timestamps                                                     | `convex/contracts/mutations.ts:22-32` — `activatedAt` / `deactivatedAt`                                                                                                                                                                                                                                                  |
| Per-contract capacity derived from underwriting tier                                 | `convex/contracts/pricing.ts:78` — `availableGuaranteeCents = rentCents * coverageCeilingMultiplier`                                                                                                                                                                                                                     |
| Platform exposure aggregate (36x worst case)                                         | `convex/contracts/aggregate.ts` `ativoInsuredCentsPlatform`, `aggregateWrites.ts`, `backfill.ts`                                                                                                                                                                                                                         |
| Test suite: 144 cases incl. exhaustive 3x3 machine matrix and 52 db-backed scenarios | `machine.test.ts`, `domain.test.ts`, `mutations.test.ts`, `scenarios.test.ts` (2653 LOC), `useCases.test.ts`                                                                                                                                                                                                             |
| Batched-cover behavioural contract already specified in tests                        | `convex/delinquencies/scenarios.test.ts:1796` (batch), `:1887` (batch idempotency), `:1919` (mixed batch)                                                                                                                                                                                                                |
| Cover-draw design accepted as ADR                                                    | `docs/architecture/decisions/0004-pilot-cover-default-coverage-draw.md` — state model, `drawId = canonical(guaranteeId, coveragePeriod)`, `ref_hash = SHA-256(drawId)` derived-never-input, `by_idempotency`, read-before-insert guard, BigInt money invariant, reconciliation cron, 6-step build sequence (lines 89-97) |
| Default-request lifecycle specified                                                  | `docs/architecture/admin.md:132-166` — 7 states, 5-step actor diagram                                                                                                                                                                                                                                                    |
| Guarantee lifecycle spec written (sibling repo)                                      | `mutav-finance/mutav` PR #185, merged 2026-07-29, now at `docs/operation/30-contract-cycle.md` section "A garantia e o recebível" — 7 states, 13 transitions with guards and evidence sources, 3 capacity policies (recommends `reserve_and_release`), 26-scenario matrix, legal-defence checklist                       |

### In progress

| Item                                                                    | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PR #246 — capacity accounting on `origin/feat/52-delinquencies-backend` | **OPEN draft, conflicting, 32 commits behind main, blocked since 2026-07-17 on owner sign-off of the money-flow semantics.** All three review blockers were fixed in commit `838515b`. Contains `convex/delinquencies/transitions.ts` (132 LOC) — `adjustContractGuarantee()`, earmark on provision / consume on paid / release exactly `appliedGuaranteeDecrementCents` on close-without-payout — the only working capacity-consumption implementation in the repo's history. Do not merge; harvest ~280 LOC. |

### Required

| Gap                                                            | Note                                                                                                                                                                                  |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `convex/guarantees/machine.ts`                                 | 6-state guarded machine. Referenced by `convex/delinquencies/machine.ts:6` as an existing sibling; the directory does not exist. Largest single item; everything else depends on it.  |
| `contracts.guaranteeState` schema field                        | Plus reseed (pre-production policy: wipe + reseed, no migration)                                                                                                                      |
| `activate` mutation (`pendente → ativo`)                       | No production writer exists; model on `cancelProposal`                                                                                                                                |
| `close` mutation (to `encerrado`)                              | No production writer exists                                                                                                                                                           |
| Affirmative "verify default" mutation                          | Today verification collapses into dismissal/dispute                                                                                                                                   |
| Capacity derived from lifecycle events                         | `availableGuaranteeCents` is never decremented on cover. Port `transitions.ts` from PR #246.                                                                                          |
| Lifecycle spec document in **this** repo                       | `mutav` is private; a grant reviewer cannot open it. Copy `mutav#185` blob `5cac711` to `docs/specs/guarantee-lifecycle.md`, trim the receivable machine to an out-of-scope appendix. |
| Delete or replace `convex/contracts/mutations.ts:updateStatus` | Unguarded, untested, zero callers                                                                                                                                                     |
| Agency-side transitions not audited                            | Three `TODO(audit)` markers on `openNotice` / `markResolved` / `markCanceled`                                                                                                         |

---

## 2. Agency Transparency Dashboard (testnet, Stellar-aware)

### Done

| Item                                               | Evidence                                                                                                                                                                                                                                      |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Route shipped, SSR-preloaded, live-updating        | `apps/agency/src/app/[locale]/(app)/transparency/page.tsx` — parallel `preloadQuery` of 3 queries + graceful null fallback; `loading.tsx`; nav entry `agency-nav.tsx:23`; legacy `/health` to `/transparency` redirect at `next.config.ts:94` |
| Active + pending guarantee counts, real data       | `convex/transparency/useCases.ts` `getContractAggregates` feeding `contracts-panel.tsx`, O(log n) via `contractsByStatusPlatform`                                                                                                             |
| Total guaranteed exposure computed                 | `ativoInsuredCentsPlatform`, `sumValue = availableGuaranteeCents + rentCents * exitCostMultiplier` (30x + 6x)                                                                                                                                 |
| Capacity utilization ratio rendered                | `apps/agency/src/components/transparency/capacity-panel.tsx:14-17`                                                                                                                                                                            |
| On-chain testnet reserve read, key-free            | `convex/reserve/actions.ts` — Soroban RPC `simulateTransaction` from an all-zero source account; `approved_assets()`, `balance()`, `symbol()`, `decimals()`                                                                                   |
| BRL pricing via live BCB PTAX with 14-day lookback | `convex/reserve/actions.ts` `fetchPtaxUsdBrl`                                                                                                                                                                                                 |
| BigInt-exact cents math                            | `convex/reserve/domain.ts:53+` `rawBalanceToCents`, `assetValueCents`                                                                                                                                                                         |
| Snapshot persistence + 15-minute cron              | `convex/schema.ts:797` `reserveSnapshots`; `convex/crons.ts`                                                                                                                                                                                  |
| Fail-closed discipline — never a mock number       | `convex/transparency/useCases.ts:72` — `available: false` on unconfigured contract, RPC failure, FX failure, or unpriced holdings                                                                                                             |
| Stellar Expert link, typed into the domain         | `convex/transparency/useCases.ts:49-57`; `ReserveCoverage` in `domain.ts`                                                                                                                                                                     |
| Testnet reserve vault address committed            | `convex/lib/env.ts:147` — `CBDGKVRP5MYER3I2WZ7F2FJULFFXY3NHB5MU75VSEZHDXYJNAB3YC7Y2`                                                                                                                                                          |
| Vault deployed, funded and exercised on testnet    | `mutav-stellar/docs/demos/2026-06-10-postpivot-stage1-vault-simulation.md` — 12/12 scenarios pass, each with a public Stellar Expert tx link; funded with 1M mock-USDC and mock-BRLT                                                          |
| Platform activity timeline                         | `contracts.useCases.getActivityByPeriod`, weekly granularity                                                                                                                                                                                  |
| i18n complete, both locales                        | `apps/agency/messages/{en,pt-BR}.json`, namespace `transparency`                                                                                                                                                                              |

### In progress

Nothing in flight. Feature work has been frozen since 2026-06-16 (PR #179). Issue **#168** remains open — the default-rate stub is its unclosed part.

### Required

| Gap                                                 | Effort / note                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Default rate — hard-coded `null`                    | `convex/transparency/useCases.ts:43`. Blocked on Deliverable 1: no contract-level default state exists, and `delinquencies` is not read by `transparency` at all.                                                                                                                                             |
| Verified-default exposure                           | Backend; blocked on Deliverable 1                                                                                                                                                                                                                                                                             |
| Coverage ratio (reserve / exposure)                 | One division. Both inputs already reach the same layout (`transparency-page.tsx:31-56`).                                                                                                                                                                                                                      |
| Reserve address rendered as text                    | `reserve-panel.tsx:63-70` — currently link-only. ~15 min.                                                                                                                                                                                                                                                     |
| Per-asset breakdown + explicit "Testnet" badge      | Data already in `reserveSnapshots`; widen `ReserveCoverage` to expose `assets[]`                                                                                                                                                                                                                              |
| Total guaranteed as a BRL card                      | `sumInsuredCents` already returned, never rendered                                                                                                                                                                                                                                                            |
| Remaining capacity as an absolute figure            | i18n keys `capacity.used` / `capacity.of` exist and are unused                                                                                                                                                                                                                                                |
| Capacity ceiling derived from a documented rule set | `convex/lib/env.ts:96-107` reads `MAX_GUARANTEE_CAPACITY_CENTS`, default `500_000_000`, unrelated to the reserve. Also missing from `.env.example`.                                                                                                                                                           |
| **Metric definition document**                      | Does not exist in any repo. Skeleton available: `mutav-pulse/docs/concepts/solvency-and-coverage.md` (formulas incl. `free_capital = max(0, stable_assets - coverage_required)`), publication discipline from `mutav/docs/operation/40-reserve.md`. Note `K` is an open decision (`mutav` open-questions 9c). |
| External reviewer access                            | Both queries use `queryWithAuth` (`convex/lib/auth.ts:55-59`). An unauthenticated reviewer sees a dash on every panel. Needs a public read path or a provisioned demo account.                                                                                                                                |
| SOW wording vs implementation                       | The SOW says Horizon and "public Stellar address"; the implementation uses Soroban RPC against a **contract id** (`C...`, not `G...`). Amend the SOW wording — the built version is stronger.                                                                                                                 |

---

## 3. MUTAV Reserve Admin Panel (testnet, async/batched cover)

### Done

| Item                                                              | Evidence                                                                                                                                                                                            |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Admin app shell, production-grade                                 | `apps/admin` — Auth0 gate with three-way routing and fail-closed re-gate per page (`(admin)/layout.tsx`), access-denied screen, i18n both locales, nav, app shell, Convex token bridge              |
| Staff authorization core                                          | `convex/lib/auth.ts` — `queryWithMutavStaff` / `mutationWithMutavStaff` / `queryWithMutavRole` / `mutationWithMutavRole`; ladder `support < compliance < admin`, `treasury` deliberately off-ladder |
| Staff management screen                                           | `apps/admin/.../staff/page.tsx` + `mutavStaff.useCases.{listAllStaff, createStaffRole, deleteStaffRole}`                                                                                            |
| Onboarding review queue (template for a queue-with-action screen) | `apps/admin/.../(admin)/page.tsx` + `review-queue.tsx`                                                                                                                                              |
| Defaults-ready-for-cover queue — **backend**                      | `convex/delinquencies/useCases.ts:336` `listOpenAdminQueue` — role >= `compliance`, paginated, cross-agency, FIFO, index-native on `by_status_openedAt`                                             |
| Individual cover recording — **backend**                          | `convex/delinquencies/mutations.ts:318` `staffMarkResolvedByCover` — persists `resolvedByUserId`, `resolvedAt`, `coverOperationPublicId`, `note`                                                    |
| Hash-chained audit of staff writes                                | `convex/audit/` + `mutavAuditLog`; `AUDIT_ACTION.DELINQUENCY_RESOLVED_BY_COVER` registered (`convex/audit/domain.ts:68`); daily Merkle anchor to Stellar via MEMO_HASH                              |
| Agency-side default reporting UI, wired                           | `apps/agency/src/components/delinquencies/*` (810 LOC) — `listByAgency`, `getByPublicId`, `openNotice`, `markCanceled`; `cover_committed` already has a user-facing label                           |
| Route + nav slot reserved                                         | `apps/admin/src/components/admin-nav.tsx:23`, i18n `nav.main.defaults`                                                                                                                              |
| Wallet connect with ownership proof                               | `packages/wallet` + `apps/admin/src/components/wallet/connect-wallet.tsx`                                                                                                                           |
| Signing architecture decided                                      | `docs/architecture/decisions/0005-wallet-signing-architecture.md`                                                                                                                                   |
| On-chain cover primitive available                                | `mutav-stellar` `reserve_vault.withdraw(asset, amount, destination, ref_hash)`; `ref_hash` documented as "guarantee + month" for off-chain correlation against Convex                               |
| "Verified vs executed" separation already modelled on-chain       | `mutav-stellar` `Fund.cover_default` (accounting only, moves no token) + `record_offchain_payout`; `mutav` 30-contract-cycle states that the on-chain debit precedes the Pix                        |

### In progress

Nothing in flight. Issue **#286** (Story 1.8 — Cobertura & sinistro) is the home issue, opened 2026-08-06, zero PRs against it.

### Required

| Gap                                            | Note                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/defaults` screen                             | `apps/admin/.../(admin)/defaults/page.tsx` is a 5-line `PillarPlaceholder`. Wiring `listOpenAdminQueue` + `staffMarkResolvedByCover` + `staffMarkCanceledByDismissal` is scoped at ~90 min in the deleted handoff note (`git show 8acd215:.claude/notes/delinquencies-domain-handoff.md`); a full table + dialogs ~1-2 days.                                            |
| `/treasury` screen                             | Same placeholder. `contracts.useCases.getInsuredCapacityGlobal` and `transparency.useCases.getReserveCoverage` already exist; the agency `capacity-panel` / `reserve-panel` are directly copyable. ~half a day.                                                                                                                                                         |
| `coverOperations` table                        | Does not exist. Seam already cut and labelled: `convex/schema.ts:469-471` and `convex/delinquencies/mutations.ts:321-323` `TODO(link): migrate to v.id("coverOperations")`. Suggested shape: `{publicId, kind, status: recorded/executed, operatorUserId, createdAt, executedAt?, reference, note, totalCents, noticeCount}` plus `by_publicId`, `by_status_createdAt`. |
| Batch cover mutation                           | `staffMarkResolvedByCover` takes exactly one notice. Convex mutations are transactional, so all-or-nothing is free. Batch semantics are already covered by `scenarios.test.ts:1796`.                                                                                                                                                                                    |
| **"Verified" vs "payout executed" separation** | The SOW's core idea. Today `staffMarkResolvedByCover` collapses intent and completion into one transition, and the notice machine has no `verified` state. Cheapest path: keep the notice machine, put the two-phase lifecycle on `coverOperations` (`recorded` to `executed`).                                                                                         |
| `defaults` / `cover` i18n namespace            | Absent from `apps/admin/messages/*.json`                                                                                                                                                                                                                                                                                                                                |
| Batch vs ADR 0004 idempotency                  | ADR 0004's key is per-guarantee-per-period; a batch spanning multiple defaults conflicts with it. Open design question.                                                                                                                                                                                                                                                 |
| SDK builder for `reserve_vault.withdraw`       | Absent in `mutav-stellar`; that repo has zero open PRs, so the work is unclaimed. Out of the SOW's critical path (payout stays manual/off-chain).                                                                                                                                                                                                                       |

**Out of scope — do not let it creep in.** ADR 0004 designs a synchronous on-chain draw with passkey quorum, reconciliation and a circuit breaker. The SOW states payout execution remains manual/off-chain, so that entire body of work is deferred. Everything Deliverable 3 needs is Tier-1 auth, which already exists.

---

## Evidence pack (SOW section 6.1)

| Artifact                                         | State                                                                                                                                                  |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Repository link                                  | Have                                                                                                                                                   |
| Lifecycle specification document                 | Written in `mutav#185`; must be **copied** into this public repo (`mutav` is private)                                                                  |
| Metric definition document                       | **Missing everywhere.** Skeleton available in `mutav-pulse`.                                                                                           |
| Test results / coverage                          | Have — 673 cases, 524 in Convex                                                                                                                        |
| GitHub milestone with closed issues              | **Missing.** All 4 milestones are closed; "Pilot" was dissolved with the note that it must not be used for new scope. A new milestone must be created. |
| Public testnet reserve address on Stellar Expert | Have — address committed, vault funded and exercised, 12/12 scenarios with public tx links                                                             |
| Walkthrough video                                | Not recorded                                                                                                                                           |
| Screenshots                                      | Not captured                                                                                                                                           |
| Demo feedback document                           | Not held. Template at `mutav/docs/events/_template/`.                                                                                                  |

---

## Cross-cutting risks

1. **`origin/feat/52-delinquencies-backend` is rotting.** 32 commits behind main; holds the only working capacity-consumption code in the repo's history. Harvest `transitions.ts` before it decays further.
2. **A guarantee state machine was written and lost.** `.claude/notes/delinquencies-domain-handoff.md` and `convex/delinquencies/machine.ts:6` both reference a `worktree-guarantee-lifecycle` that was never committed or pushed; the note points at paths on another machine. Check that worktree before it is pruned.
3. **`apps/fund` is fully mocked** (`TRANSPARENCY_STATS`, `AUM_DATA` literals, a $159M AUM series) and unauthenticated. Keep it out of every evidence artifact.
4. **SOW internal inconsistency.** The out-of-scope section states twice that the CNPJ is a deliverable of this sprint; section 4.1 lists only the three technical deliverables. Correct the document before submission.
5. **Marketing promise ahead of product.** `mutav#196` ships a site panel promising a real-time coverage ratio readable on Stellar; that portal does not exist yet.
