---
branch: test/guarantees-scenarios
category: test
summary: "db-layer scenario coverage for the new domains: `convex/guarantees/scenarios.test.ts` and `convex/leases/scenarios.test.ts` exercise schema conformance for every field and literal, every index with a cross-agency isolation read, the capacity invariant `available + reserved = ceiling`, the immutable `terms` snapshot, and the one-open-guarantee-per-lease rule with its `leases.openGuaranteeId` pointer; also pins the Turbopack workspace root so a worktree under `.claude/worktrees/` stops resolving the outer lockfile and 404ing every route"
sync_actions: []
---
