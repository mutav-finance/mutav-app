{
  "name": "@mutav/fund",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@mutav/app-shell": "workspace:*",
    "@mutav/i18n": "workspace:*",
    "@mutav/ui": "workspace:*"
  },
  "devDependencies": {
    "@mutav/tsconfig": "workspace:*"
  },
  "scripts": {
    "dev": "next dev --port 3002",
    "build": "next build",
    "start": "next start --port 3002",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
